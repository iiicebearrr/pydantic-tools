{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "pytest cov",
            "type": "shell",
            "command": "pytest --cov=pydantic_tools/ tests/",
        },
        // cov report html
        {
            "label": "pytest cov html",
            "type": "shell",
            "command": "pytest --cov=pydantic_tools/ --cov-report html tests/",
        },
    ]
}